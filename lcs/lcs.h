#pragma once

#include <string>
#include <vector>
#include <iostream>

// Find the longest common subsequence of two strings.
std::string LCS(const std::string &first, const std::string &second);

std::string naive(const std::string &first, const std::string &second);

